// 入力値
// 条件
// 0 <= h <= 24 24を超えた場合は0
// 0 <= m <= 60 60を超えた場合は0
// 12:30
let th = ['12'];
let tm = ['30'];

th = (th + Math.floor((tm + 30) / 60)) % 24;
// 60の乗算で60を超過したばあい、超過分のみ表示
tm = (tm + 30) % 60;
th = String(th);
tm = String(tm);
